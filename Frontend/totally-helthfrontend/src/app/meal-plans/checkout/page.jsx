import Checkout from "@/app/pages/meal-plans/checkout/Checkout";
import React from "react";

export const metadata = {
  title: "Checkout",
  description: "Checkout",
};

const page = () => {
  return <Checkout />;
};

export default page;
